export enum ClientRouterLink {
  Home = "",
  Practice = "/practice",
  Exam = "/exam-list",
  ExamDetail = "/exam-information",
  ExamTest = "/exam-test",
  ExamResult = "/exam-result",
  Result = "/result-list",
  Leaderboard = "/leader-board",
  Blog = "/blog-list",
}

export enum AuthRouterLink {
  Login = "login",
  Register = "register",
}
